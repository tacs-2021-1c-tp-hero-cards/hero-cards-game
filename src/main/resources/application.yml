spring:
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:file:./data/testdb"
    username: "sa"
    password: "sa"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    generate-ddl: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect